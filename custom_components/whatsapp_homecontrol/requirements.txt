# File: custom_components/whatsapp_homecontrol/requirements.txt
# Python dependencies for WhatsApp Control integration

# IMPORTANT: This integration requires manual setup of WhatsApp library
# See README.md for installation instructions

# Core dependencies (auto-installed by Home Assistant)
qrcode>=7.3.1
selenium>=4.0.0

# The following are already provided by Home Assistant core:
# - Pillow (image processing)
# - aiohttp (HTTP client)
# - aiofiles (async file operations)
# - voluptuous (config validation)

# WhatsApp library - MUST BE INSTALLED MANUALLY:
# Option 1 (Recommended): Use CallMeBot service (no library needed)
# Option 2: Install whatsapp-web.js with Node.js
# Option 3: Use Twilio WhatsApp API

# Optional: Audio processing for voice messages
# pydub>=0.25.1